import 'package:flutter/material.dart';

class SingleParkPage extends StatelessWidget {
  const SingleParkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(),
    );
  }
}
